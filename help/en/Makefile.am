helpdir = ${pkgdatadir}/help/en
html_helpfiles = $(wildcard en/*.html) $(wildcard en/*.css) $(wildcard en/img/*.png)
dist_help_DATA = $(html_helpfiles)

helpimgdir = ${pkgdatadir}/help/en/img
img_help = $(wildcard en/img/*)
dist_helpimg_DATA = $(img_help)
